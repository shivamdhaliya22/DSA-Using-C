#include<stdio.h>

int main() {
    
    int arr[] = {10, 20, 30, 40, 50};
    int n = 5;

    // Traverse the array using an loop
    for (int i = 0; i < n; i++) {

        printf("%d ", arr[i]);
    }
    
    return 0;
}